-- The following has to be executed in SNOWFLAKE CLI

-- Login to snowsql in snowflake CLI
-- snowsql -a [Account Identifier] -u [User Name]

-- USE DATABASE [DATABASE NAME]
-- USE SCHEMA [SCHEMA NAME]

-- use role and warehouse if required
-- USE ROLE ACCOUNTADMIN
-- USE WAREHOUSE COMPUTE_WH

-- upload file using snowsql

-- Use <folder path> without spaces to avoid errors
put file://<folder path>\MOCK.csv @named_stage AUTO_COMPRESS = FALSE [OVERWRITE = TRUE];

______________________________________________________________

-- to check if it snowflake CLI is installed
snow --help 

-- creating connection to snowflake
snow connection add

--credentials to connect to snowflake
Enter connection name: my_connection
Enter account: xxxxxxxxx
Enter user: xxxxxxxx
Enter password:
Enter role: xxxxxxx
Enter warehouse: COMPUTE_WH
Enter database: LA_DB
Enter schema: LA_SCHEMA
--Optional Fields
Enter host:
Enter port:
Enter region:
Enter authenticator:
Enter workload identity provider:
Enter private key file:
Enter token file path:

-- to set default connection
snow connection set -default my_connection

-- some generatl queries
snow sql --query "select * from LA_DB.LA_SCHEMA.CUSTOMER"

snow object list database



